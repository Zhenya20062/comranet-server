export class MemberData {
    username:string;
    photo_url:string;

    constructor(username:string, photo_url:string) {
        this.username = username;
        this.photo_url = photo_url;
    }
}